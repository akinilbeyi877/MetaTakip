<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage -->
    <external-path name="external_files" path="." />

    <!-- External files directory -->
    <external-files-path name="my_external_files" path="." />

    <!-- Cache directory -->
    <external-cache-path name="my_external_cache" path="." />

    <!-- Internal files directory -->
    <files-path name="my_internal_files" path="." />

    <!-- Cache directory -->
    <cache-path name="my_internal_cache" path="." />

    <!-- External media directory -->
    <external-media-path name="external_media" path="." />

    <!-- PDF klasörü -->
    <external-path name="pdf_files" path="Android/data/com.example.metatakip/files/pdfs" />
</paths>