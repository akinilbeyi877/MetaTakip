<!-- ══════════════════════════════════════════════════════
     ✅ SYNC BANNER — activity_home.xml içinde BU BLOĞU BULUN
     ve aşağıdaki ile TÜMÜYLE DEĞİŞTİRİN
     (android:id="@+id/layoutSyncBanner" olan LinearLayout)
     ══════════════════════════════════════════════════════ -->

<LinearLayout
    android:id="@+id/layoutSyncBanner"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:background="#E8F5E9"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:visibility="gone">

    <!-- Üst satır: durum mesajı + sağ ok -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tvSyncBanner"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Senkron durumu"
            android:textStyle="bold"
            android:textColor="#2E7D32"
            android:textSize="13sp" />

        <!-- Tıklanabilir oku göster -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="›"
            android:textColor="#2E7D32"
            android:textSize="20sp"
            android:paddingStart="6dp" />

    </LinearLayout>

    <!-- Alt satır: kaynak cihaz | gmail | klasör (otomatik dolar) -->
    <TextView
        android:id="@+id/tvSyncDetail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:text=""
        android:textColor="#388E3C"
        android:textSize="11sp"
        android:visibility="gone" />

</LinearLayout>
